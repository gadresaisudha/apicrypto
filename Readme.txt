First install npm on your local from https://nodejs.org/en/download/
Then use npm -v command to check the installation of npm
In order to use npx use the following command
npm i -g npx
In order to create react app use the following command
npx create-react-app apicrypto  
Now we have our app apicrypto where we can make changes and run it using different commands such as npm start           